# waterandearth

Static house-build budget app.

## Features

- Editable construction budget table
- Local browser save
- JSON backup and restore
- Excel `.xls` export
- CSV import
- Budget summary and charts
- English and Vietnamese interface
- PWA manifest and service worker for install/offline support

## Deploy

Upload these files as the `waterandearth` site/app:

- `index.html`
- `manifest.json`
- `service-worker.js`
- `assets/icon.svg`

Any static host works, including Netlify, Vercel static hosting, GitHub Pages, S3, or a basic web server.
